CREATE PROCEDURE [dbo].[SpGetAllPromotions]   @Page int  AS  BEGIN   SELECT * FROM    Promotion   ORDER BY promotion_id   OFFSET (@Page - 1) * 10 ROWS    FETCH    NEXT 10 ROWS ONLY    FOR JSON PATH   END